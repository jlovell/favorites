#ios-native-bar.navbar-inverse

:css
  #ios-native-bar {
    width: 100%;
    height: 20px;
    display: none;
  }

:javascript
  $(document).on('ready page:load', function () {
    if (window.navigator.standalone) {
      $("#ios-native-bar").show();
    }
  })
